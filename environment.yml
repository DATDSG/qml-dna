# Auto-generated from results/appendix/environment.json
# Create with: conda env create -f environment.yml
# Update with: conda env update -f environment.yml --prune

name: qml-dna
channels:
  - conda-forge
  - defaults

# Pin the base Python version captured
variables: {}
dependencies:
  - python=3.10.13
  - pip
  - pip:
      - numpy==2.1.3
      - pandas==2.3.1
      - scikit-learn==1.7.1
      - biopython==1.85
      - matplotlib==3.10.0
      - pennylane==0.42.1
